{
  "author": "beame",
  "version": "0.0.1",
  "name": "typescript-template",
  "description": "Template for a simple typescript function (compatible with cdk lambda)",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "npx tsc",
    "clean": "rm -rf **/*.js **/*.js.map node_modules/",
    "setup": "npm run clean && npm install && npm run build",
    "lint": "npx eslint . --ext .ts",
    "test": "npm run build && npx jest"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.13",
    "@typescript-eslint/eslint-plugin": "^8.8.0",
    "@typescript-eslint/parser": "^8.8.0",
    "eslint": "^9.11.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
